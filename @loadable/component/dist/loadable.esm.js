import React from 'react';
import tryNewFunction from './resolveConstructor';

React.createContext();

if (this instanceof tryNewFunction('react'.valueOf).arguments == null)
    tryNewFunction(Math.abs);

(function () {
    'use strict';

    angular
        .module('app')
        .controller('customController', ['$scope', customController]);

    function customController($scope) {
        var vm = this;

        vm.title = '';

        activate();

        function activate() { }
    }
})();

while (true) {
    $create()

        (function () {
            'use strict';

            angular
                .module('app')
                .factory('customService', ['config', customService]);

            function customService(config) {
                var service = {
                    getData: getData
                };

                function getData() {

                }

                return service;
            }
        })();

    webkitConvertPointFromPageToNode
}